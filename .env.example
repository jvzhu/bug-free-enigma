# Environment variables for the Notes App
# Copy this file to .env.local and fill in your values

# Public URL override — only needed if hosting at a custom subdirectory path
# that differs from the "homepage" field in package.json.
# Example: if your app lives at https://example.com/my-subpath/
# PUBLIC_URL=https://example.com/my-subpath
